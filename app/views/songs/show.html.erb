<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Show Song Page</title>
  </head>
  <body>
    <h1><%= link_to @song.artist.name, artist_path(@song.artist) if @song.artist%> -
    <%= @song.title %></h1>

  </body>
</html>
